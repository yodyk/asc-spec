/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The design system ships as a single global stylesheet (src/app/globals.css),
  // ported from the approved prototype — no Tailwind.
};

export default nextConfig;
